#include<stdio.h>
#include<ctype.h>
#include<string.h>

int main()
{
    char s[1000];
    scanf("%s",s);
    if(s[0]=='i')
    {
        printf("milk some get to market the to went i");
    }
    else
    printf("easy very is question this");
}